name: Tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
        include:
          - python-version: "3.12"
            coverage: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install package + test deps (uv pip)
        run: uv pip install --system --group dev .

      - name: Run tests
        run: python -m pytest --cov-report=term-missing --cov-report html:htmlcov --cov-config=pyproject.toml --cov=tcpb --cov=tests tests/test_frontend_client.py tests/test_tcpb_client.py tests/test_utils.py

      - name: Upload coverage HTML
        if: ${{ matrix.coverage == true }}
        uses: actions/upload-artifact@v4
        with:
          name: htmlcov-py${{ matrix.python-version }}
          path: htmlcov

      - name: Build sdist (only on 3.12)
        if: ${{ matrix.coverage == true }}
        run: python -m pip install build && python -m build --sdist

      - name: Install from sdist (only on 3.12)
        if: ${{ matrix.coverage == true }}
        run: python -m pip install dist/*.tar.gz
